#include "MusicSymbolBmpStruct.h"

bmp_formatter::MusicSymbolBmpStruct::MusicSymbolBmpStruct(unsigned char red, unsigned char green, unsigned char blue, unsigned num_of_pixels)
	: red(red)
	, green(green)
	, blue(blue)
	, num_of_pixels(num_of_pixels)
{
	// empty body
}
